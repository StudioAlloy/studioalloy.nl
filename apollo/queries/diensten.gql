# query GetDiensten {
#   diensten(where: {orderby: {field: MENU_ORDER, order: ASC}}) {
#     nodes {
#       title
#       excerpt
#       link
#       slug
#       acfDiensten {
#         featured,
#         illustrationName,
#       }
#     }
#   }
# }
query GetDiensten {
  diensten(where: {orderby: {field: MENU_ORDER, order: ASC}}) {
    nodes {
      title
      excerpt
      link
      slug
      acfDiensten {
        featured
        illustrationName,
      }
    }
  }
}